# API Configuration (for local development only)
# In production, these values are retrieved from GitHub secrets
API_ENDPOINT=http://localhost:8787/api/publications
API_TIMEOUT=30
API_RETRIES=3

# Test API Configuration (for testing)
TEST_API_ENDPOINT=http://localhost:8788/api/publications
